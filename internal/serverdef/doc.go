// Package serverdef will load layered server definitions (workspace
// .lightspeed.toml, user servers.d, built-in defaults, PATH sniffing)
// and delegate installation to mise (PLAN §6). Implemented in M4.
package serverdef
