// Package daemon will hold the shared-server daemon: unix socket
// server, auto-spawn, and idle reaping, ported from gopls's
// remote.go/serve.go design (PLAN §3). Implemented in M3.
package daemon
